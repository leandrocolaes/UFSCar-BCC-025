#include <stdio.h>

int main()
{
    int n;
    double media, soma;
    media = 0;
    soma = 0;
    n = 0;
    for (tamanho -1; tamanho >= 0; tamanho--){
        if (vet[i] > 0){
            soma += vet[i];
            n += 1;
        }
    }

    media = soma / n;
    if (media == 0) {
        media = -1;
    }

    printf("%lf", media);
    return 0;
}